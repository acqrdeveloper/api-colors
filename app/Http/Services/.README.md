### ExampleService.js

```js
'use strict'

const Helper = require('app/Http/Utils/Helper')
const Model = require('app/Http/Models/Model')

class ExampleService {
  static async start({}) {
    // Query where
    // Query model
    const result = null

    // Response
    return {
      message: '',
      result: result,
    }
  }
}

module.exports = ExampleService
```
